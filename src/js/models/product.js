export default class Product {
	/**
   * id
   * type --> clothing, decoration, beautification
   * name
   * description
   * purchasePrice --> in euros with two decimals
   * sellingPrice --> in euros with two decimals
   * minimalStock
   * stock
   * color? --> hexadecimal
   * size? --> two variants clothing size and packaging size
   * weight? --> kilograms
   * pieces? --> products per package
   */
}
